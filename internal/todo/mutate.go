package todo

// The mutating actions (plan §6.3, Task 4): add/addm/addto, append, prepend,
// pri, depri, do, del, replace, move, archive, and report, with todo.sh-exact
// messages and file-state effects, plus the file store that creates TODO_DIR
// and the todo/done/report files on demand (§6.5).
