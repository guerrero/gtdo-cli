package todo

// The _format listing pipeline (plan §6.2, Task 4): numbering with padding
// to the width of the total line count, filters, sort, per-word coloring, and
// the `PREFIX: N of M tasks shown` summary, honoring plain mode and the
// -@/-+/-P hide toggles.
